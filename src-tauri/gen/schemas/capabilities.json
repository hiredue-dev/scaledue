{"default":{"identifier":"default","description":"Core permissions for the main window. Without this file the frontend has NO permissions: custom invoke() commands still work (they are not ACL-gated), but core plugin calls such as event.listen are silently denied, so no backend events ever reach the UI.","local":true,"windows":["main"],"permissions":["core:default","opener:default","dialog:default","dialog:allow-confirm","dialog:allow-ask"]}}